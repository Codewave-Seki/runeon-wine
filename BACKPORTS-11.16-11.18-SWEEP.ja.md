# Wine 11.16–11.18 ユーザーモード修正の一括バックポート

[English](BACKPORTS-11.16-11.18-SWEEP.md) | [简体中文](BACKPORTS-11.16-11.18-SWEEP.zh-CN.md) | [日本語](BACKPORTS-11.16-11.18-SWEEP.ja.md)

> 英語版が正です。中国語版と日本語版は便宜上の完全な翻訳です。

候補 `cx26.3-wine11.0-runeon.11` は `0106` の後に 4 つのアップストリームパッチを追加します。

| パッチ | 内容 |
|---|---|
| `0062-wine-11.16-targeted-fixes.patch` | `wine-11.16` で初めてリリースされた WineHQ コミット 20 件 |
| `0063-wine-11.17-targeted-fixes.patch` | `wine-11.17` のコミット 107 件 |
| `0064-wine-11.18-targeted-fixes.patch` | `wine-11.18` のコミット 77 件 |
| `0065-wine-11.18-imm32-ime-keydown-lparam.patch` | CrossOver の IME コードに合わせて調整した `a6b3099042` |

各統合パッチはすべてのアップストリームコミットと作者を列挙し、manifest は完全なコミット ID を記録します。`0045`–`0061` に含まれる 13 件は重複させていません。本番コードの調整は `0064` の 1 件のみです。`d22810d47b` はこのベースにない `intsafe.h` を読み込むため、`bmpdecode.c` にオーバーフローの約束事が同じローカルの `UIntMult()` を定義しました。これは完全な製品ビルドで判明したもので、パッチがきれいに適用できるかだけでは見つかりません。

## 選定基準

`wine-11.15..wine-11.18` の全 1,009 コミットを検討しました。次の条件をすべて満たすコミットのみを採用しました。

- Steam、ランチャー、インストーラー、ゲームが読み込むユーザーモード DLL を変更するもの。メディア、入力、音声、COM、ネットワークと TLS、暗号、WMI、テキスト、画像と D3DX、D3D9 経路、インストーラー、UI コントロールが該当します。
- `ntdll`、`server`、`win32u`、`winemac.drv`、`wow64`、`loader`、`configure`、`tools`、`libs` を変更せず、サーバープロトコルヘッダーにも触れないもの。
- アップストリームの順序で、パッチ `0001`–`0106` の上にファズなしで適用できるもの。完全な系列を再適用すると、レビュー済みツリーとファイル単位で一致します。

適用できた 271 件のうち、テキスト上はきれいに適用できても、このベースでは誤った動作になる以下を除外しました。

| 除外 | 理由 |
|---|---|
| NUMA（`073a4edad7`、`6ea56eb539` と FIXME） | このベースの ntdll が実装していない `SystemNumaProcessorMap` を呼び出し、成功を返していたスタブが失敗するようになる |
| UI 言語の再実装（`5b4f8aa35b`、`480b8e4601`、`fa7d1b1571`、`6c91a3f4e7`、`74858bee70`、`065f1ad633`）、コンソール入力の書き換え `8126bd7f9c` | 除外した ntdll の変更に依存する、ロケール／コンソールのグローバルな動作。ゲームの言語判定を黙って変えてはならない |
| PnP 識別系列（`hidclass.sys`、`winebus.sys` 互換 ID、`mmdevapi`/`coreaudio` インスタンス ID、`IRP_MJ_CREATE` ハンドラー、setupapi の cfgmgr32 化） | 除外した `ntoskrnl`/`mountmgr` の変更に依存し、コントローラーやゲームが列挙するデバイス識別を変える |
| `msvcrt` 標準ハンドル系列、`user32` オブジェクトのセキュリティ記述子 | プロセス全体の stdio／オブジェクト作成の動作で、一部はサーバー側の変更に依存 |
| D3D11 `Discard*`／リソース共有と関連する wined3d のリファクタリング、`WINED3D_TEXTURE_GENERATE_MIPMAPS` とそれに依存する d3d9 修正、D3DX10 スプライト実装、quartz フルスクリーンエミュレーション、WMA デコーダーと wg_parser のフラグ／PTS 系列 | 機能またはリファクタリングの系列で、Runeon に必要のないグラフィックス・動画・ウィンドウの動作変更、または除外した連鎖の一部に依存する |
| secur32 の LSA/negotiate 変更 | 除外した msv1_0/kerberos/lsass の作業に属する。secur32 の SChannel と一般的な修正は採用 |
| `mfreadwrite` `4d22860530` | `.9` で延期した非同期コマンドの参照カウント系列に属する |
| ツールバー、シェルビュー、ボリュームラベルなどの小変更 | Steam やゲームへの利点がない |

以前に延期した CoreAudio 周期、WGI 初期化、XAudio2 のアンロック、MF drain／ライフサイクルのコミットは、[BACKPORTS-11.16-11.17.ja.md](BACKPORTS-11.16-11.17.ja.md) の理由により引き続き延期します。

アップストリームの revert `e5abb7c03a` は、x64 `RaiseException` で非揮発性レジスタを保持する実装を削除します。この実装はこのベースに存在し、一部の DRM が依存しているとコメントに記されています。この revert は Wine の欠陥ではなく Windows 11 25H2 のテスト結果を根拠としているため、採用しません。

## このベースには該当しないもの

一部の修正は CrossOver 26.3 に存在しないコードを対象としています。例として、msxml3 の XPath エンジン、WinHTTP `WINHTTP_OPTION_SERVER_CBT`、SymCrypt ベースの bcrypt/rsaenh 経路、新しい d2d1 コマンドリストと dsound リサンプラーのコード、cfgmgr32 があります。wined3d スワップチェーンのミューテックス修正と wg_parser の初期ギャップ修正が対象とする障害は、このベースにはもともとありません。これらはいずれも調整していません。

## 検証

`scripts/static-check.sh` と `scripts/integration-check.sh` は成功しました。固定アーカイブにパッチ系列を適用して再構築したツリーは、レビュー済みツリーとファイル単位で一致します。追加された低レベル呼び出しを一つずつ確認し、導入されるのは汎用のヒープとクリティカルセクションの関数のみでした。製品としての完全な Wine ビルドはローカル seed 候補で実施します。この一括バックポートは、特定のゲームの問題を修正したとは主張しません。

これは選別したバックポートであり完全な監査ではないため、`upstreamAuditThrough` は `wine-11.15` のままです。
